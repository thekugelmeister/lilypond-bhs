\version "2.20"

%% TODO: Rename either this file or these methods/objects
%% TODO: It's confusing that this is off on it's own, but there are relevant methods in staff-spec.ily...

#(define staves '())

#(define (set-staves! staves-list)
  (set! staves staves-list))
